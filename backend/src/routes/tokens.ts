import { Router } from 'express';

export const tokensRouter = Router();

// Placeholder routes - will be implemented in tokens API task
tokensRouter.get('/', (req, res) => {
  res.json({ message: 'Tokens routes - coming soon' });
});